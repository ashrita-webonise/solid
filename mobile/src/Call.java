public class Call extends Contacts(){
	public void makeCall(){
		/* check if contact exists then launch the calling action
		 * if contact does not then launch dailPad;
		 */
	}
	public void videoCall(){
		/* check if contact exists if yes then
		 * Launch front camera and initiate the call
		 * 
		 */
	}
	
}
