public class Message extends contacts(){
	public void sendText(){
		/* check if contact exists then launch the messaging action
		 * if contact does not then launch add a contact;
		 */
		
		/* when the character count of the text exceeds 320 then append another
		 * text field to the existing one
		 */
	}
	public void receiveText(){
		/*	display sender information
		 * and display text
		 */
	}
}
