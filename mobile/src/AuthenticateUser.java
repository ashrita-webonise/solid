public class AuthenticateUser(){
	
	/* check settings and fetch the type of lock set by user */
		public void OpenLock(){
			if lock == pin
			/* user input pin and verify with stored pin
			 * 
			 * if(success) call MainPage();
			 * 
			 * else if loop back but only 10 times
			 * 
			 * else when crossed the no of attempts initiate 
			 * password retrival through phone owner's registered account
			 */
		
			//else if Pattern	:repeat the same flow as for pin
			
			//else if code		:repeat the same flow as for pin
			
			//else swipe off and open
		}
}