
public class LoginAccount extends loginPage(SocialMedia) {
	
	enum Medias {facebook, LinkedIn, GitHub};
	String Username, password;
	
	for (SocialMedia:Medias){

		/* Accept the username and password from user
		 * and call Login Authentication by passing the parameters
		 * socialmedia,	username, password
		 */
	}
	LoginCredentials(SocialMedia,username,password)
		
		/* for social media present in the enum
		 * call LoginCredentials by sending the 
		 * username and parameters
		 */

}