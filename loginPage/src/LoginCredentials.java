
public class LoginCredentials extends loginPage(SocialMedia,username,password){
	
	/*	Establish connection with the respective Social Media
	 * 	and pass the username and password to it
	 * 	
	 * 	if a valid user then call nextPage();
	 * 
	 * 	else if credentials are invalid then call
	 * 	LoginCredentials(SocialMedia,username,password) again
	 * 
	 * 	else user does not have an account in that social media
	 * 	call createAccount(SocialMedia);
	 */
	
}
